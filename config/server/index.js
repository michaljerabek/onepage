module.exports = {
    Db: {
        global: {
            name: "global",
            host: "localhost"
        },
        users: {
            host: "localhost"
        }
    },

    websocket: {
        dbTimeout: 60 * 1000
    },

    appHostname: "app.dev"
};
